---
title: "08：混合检索-稠密+稀疏检索融合"
description: "RAG 学习笔记第 8 篇：学习稀疏向量、密集向量、RRF、加权融合，以及基于 Milvus 和 BGE-M3 的混合检索实现。"
pubDate: 2026-06-29
tags: ["RAG", "AI工程"]
series: "RAG学习笔记"
---

> 本文整理自 Mini RAG 课程的 C3 检索技术优化，保留原始代码和关键说明，并按博客合集顺序归档。

# 混合检索-稠密+稀疏检索融合

单独用关键词检索容易“只看字面”，单独用语义向量检索容易“忽略精确词”，所以把两者结合起来，提高检索的准确率和召回率。

```python
用户查询 Query（查询）
        ↓
BGE-M3 生成向量
        ↓
┌────────────────────┬────────────────────┐
│ 稀疏向量 Sparse     │ 密集向量 Dense      │
│ 关键词匹配          │ 语义匹配            │
└────────────────────┴────────────────────┘
        ↓                    ↓
 稀疏检索结果列表      密集检索结果列表
        ↓                    ↓
        └────── RRF / 加权融合 ──────┘
                         ↓
                 最终排序结果 Top-K
                         ↓
              输入给 RAG 生成回答
```

# 稀疏向量

稀疏向量可以理解为 **关键词匹配型向量**，它通常维度很高，每个维度对应词表中的一个词，但大多数位置都是 0。比如词表有 5 万个词，那么一个文本可能只命中其中几十个词，所以大部分维度为空。

| 方面 | 内容 |
| --- | --- |
| 本质 | 基于词频、关键词、词袋模型 |
| 代表方法 | TF-IDF（词频-逆文档频率）、BM25（经典排序算法） |
| 优点 | 可解释性强、无需训练、适合专业术语和精确词匹配 |
| 缺点 | 不懂语义，无法理解同义词、近义词 |
| 典型问题 | 搜索“番茄炒鸡蛋”时，可能无法匹配“西红柿炒蛋” |

# 密集向量

密集向量可以理解为 **语义理解型向量**。它由深度学习模型生成，例如 Word2Vec、GloVe、BERT（双向编码器表示模型）、GPT（生成式预训练模型）等。**密集向量会把文本映射到一个语义空间中，使语义接近的文本在向量空间中距离更近。**

| 方面 | 内容 |
| --- | --- |
| 本质 | 深度学习生成的语义向量 |
| 代表方法 | Word2Vec、GloVe、BERT、GPT Embedding（嵌入） |
| 优点 | 能理解同义词、近义词、上下文关系 |
| 缺点 | 可解释性弱，需要模型和算力，对精确关键词有时不敏感 |
| 典型能力 | 搜索“蛋白质丰富的家常菜”时，可能匹配“西红柿炒蛋” |

# 混合检索

```python
用户查询
   ↓
生成稀疏向量 + 密集向量
   ↓
稀疏检索：找关键词匹配强的文档
密集检索：找语义相似度高的文档
   ↓
融合排序
   ↓
返回最终结果
```

## RRF Reciprocal Rank Fusion（倒数排序融合）

RRF（倒数排序融合）不关心不同检索系统给出的原始分数，只关心文档在每个检索结果中的排名。

RRF（倒数排序融合）的好处是 **不需要处理不同检索模型分数尺度不一致的问题**。

$$
RRF_{score}(d) = \sum_{i=1}^{k} \frac{1}{rank_i(d) + c}
$$

| 符号 | 含义 |
| --- | --- |
| (d) | 待评分文档 |
| (k) | 检索系统数量，比如稀疏检索和密集检索就是 2 |
| $(rank_i(d))$ | 文档在第 (i) 个检索系统中的排名 |
| (c) | 平滑参数，通常为 60 |

## 加权线性组合

加权线性组合是把稀疏检索得分和密集检索得分归一化后相加

$$
Hybrid_{score} = \alpha \cdot Dense_{score} + (1 - \alpha) \cdot Sparse_{score}
$$

| 参数 | 含义 |
| --- | --- |
| $(\alpha)$ | 控制密集向量得分占比 |
| $(Dense_{score})$ | 密集向量检索得分 |
| $(Sparse_{score})$ | 稀疏向量检索得分 |

## 优势与局限

| 优势 | 局限 |
| --- | --- |
| **召回率与准确率高**：能同时捕获关键词和语义，显著优于单一检索。 | **计算资源消耗大**：需要同时维护和查询两套索引。 |
| **灵活性强**：可通过融合策略和权重调整，适应不同业务场景。 | **参数调试复杂**：融合权重等超参数需要反复实验调优。 |
| **容错性好**：关键词检索可部分弥补向量模型对拼写错误或罕见词的敏感性。 | **可解释性仍是挑战**：融合后的结果排序理由难以直观分析。 |

# 代码案例

## **定义 Collection**

| 字段 | 作用 |
| --- | --- |
| pk | 主键 |
| img_id | 图像编号 |
| path | 图像路径 |
| title | 标题 |
| description | 描述 |
| category | 类别 |
| location | 地点 |
| environment | 环境 |
| sparse_vector | 稀疏向量 |
| dense_vector | 密集向量 |

```python
import json
import os
os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"
import numpy as np
from pymilvus import connections, MilvusClient, FieldSchema, CollectionSchema, DataType, Collection, AnnSearchRequest, RRFRanker
from pymilvus.model.hybrid import BGEM3EmbeddingFunction

# 1. 初始化设置
COLLECTION_NAME = "dragon_hybrid_demo"
MILVUS_URI = "http://localhost:19530"  # 服务器模式
DATA_PATH = "../../data/C4/metadata/dragon.json"  # 相对路径
BATCH_SIZE = 50

# 2. 连接 Milvus 并初始化嵌入模型
print(f"--> 正在连接到 Milvus: {MILVUS_URI}")
connections.connect(uri=MILVUS_URI)

print("--> 正在初始化 BGE-M3 嵌入模型...")
ef = BGEM3EmbeddingFunction(use_fp16=False, device="cpu")
print(f"--> 嵌入模型初始化完成。密集向量维度: {ef.dim['dense']}")

# 3. 创建 Collection
milvus_client = MilvusClient(uri=MILVUS_URI)
if milvus_client.has_collection(COLLECTION_NAME):
    print(f"--> 正在删除已存在的 Collection '{COLLECTION_NAME}'...")
    milvus_client.drop_collection(COLLECTION_NAME)

fields = [
    FieldSchema(name="pk", dtype=DataType.VARCHAR, is_primary=True, auto_id=True, max_length=100),
    FieldSchema(name="img_id", dtype=DataType.VARCHAR, max_length=100),
    FieldSchema(name="path", dtype=DataType.VARCHAR, max_length=256),
    FieldSchema(name="title", dtype=DataType.VARCHAR, max_length=256),
    FieldSchema(name="description", dtype=DataType.VARCHAR, max_length=4096),
    FieldSchema(name="category", dtype=DataType.VARCHAR, max_length=64),
    FieldSchema(name="location", dtype=DataType.VARCHAR, max_length=128),
    FieldSchema(name="environment", dtype=DataType.VARCHAR, max_length=64),
    FieldSchema(name="sparse_vector", dtype=DataType.SPARSE_FLOAT_VECTOR),
    FieldSchema(name="dense_vector", dtype=DataType.FLOAT_VECTOR, dim=ef.dim["dense"])
]

# 如果集合不存在，则创建它及索引
if not milvus_client.has_collection(COLLECTION_NAME):
    print(f"--> 正在创建 Collection '{COLLECTION_NAME}'...")
    schema = CollectionSchema(fields, description="关于龙的混合检索示例")
    # 创建集合
    collection = Collection(name=COLLECTION_NAME, schema=schema, consistency_level="Strong")
    print("--> Collection 创建成功。")

    # 创建索引
    print("--> 正在为新集合创建索引...")
    sparse_index = {"index_type": "SPARSE_INVERTED_INDEX", "metric_type": "IP"}
    collection.create_index("sparse_vector", sparse_index)
    print("稀疏向量索引创建成功。")

    dense_index = {"index_type": "AUTOINDEX", "metric_type": "IP"}
    collection.create_index("dense_vector", dense_index)
    print("密集向量索引创建成功。")

collection = Collection(COLLECTION_NAME)
collection.load()
print(f"--> Collection '{COLLECTION_NAME}' 已加载到内存。")
```

## **BGE-M3 双向量生成**

```python
#数据加载与预处理
if collection.is_empty:
    print(f"--> Collection 为空，开始插入数据...")
    with open(DATA_PATH, 'r', encoding='utf-8') as f:
        dataset = json.load(f)

    docs, metadata = [], []
    for item in dataset:
        parts = [
            item.get('title', ''),
            item.get('description', ''),
            item.get('location', ''),
            item.get('environment', ''),
        ]
        docs.append(' '.join(filter(None, parts)))
        metadata.append(item)
 
#向量生成
print("--> 正在生成向量嵌入...")
embeddings = ef(docs)
print("--> 向量生成完成。")

# 获取两种向量
sparse_vectors = embeddings["sparse"]    # 稀疏向量：词频统计
dense_vectors = embeddings["dense"]      # 密集向量：语义编码

#Collection 批量数据插入,为每个字段准备批量数据
img_ids = [doc["img_id"] for doc in metadata]
paths = [doc["path"] for doc in metadata]
titles = [doc["title"] for doc in metadata]
descriptions = [doc["description"] for doc in metadata]
categories = [doc["category"] for doc in metadata]
locations = [doc["location"] for doc in metadata]
environments = [doc["environment"] for doc in metadata]

# 插入数据
collection.insert([
    img_ids, paths, titles, descriptions, categories, locations, environments,
    sparse_vectors, dense_vectors
])
collection.flush()
```

## **实现混合检索**

```python
#查询向量生成
search_query = "悬崖上的巨龙"
search_filter = 'category in ["western_dragon", "chinese_dragon", "movie_character"]'
top_k = 5

print(f"\n{'='*20} 开始混合搜索 {'='*20}")
print(f"查询: '{search_query}'")
print(f"过滤器: '{search_filter}'")

query_embeddings = ef([search_query])
dense_vec = query_embeddings["dense"][0]
sparse_vec = query_embeddings["sparse"]._getrow(0)

#混合检索执行
# 定义搜索参数
search_params = {"metric_type": "IP", "params": {}}

# 先执行单独的搜索
print("\n--- [单独] 密集向量搜索结果 ---")
dense_results = collection.search(
    [dense_vec],
    anns_field="dense_vector",
    param=search_params,
    limit=top_k,
    expr=search_filter,
    output_fields=["title", "path", "description", "category", "location", "environment"]
)[0]

for i, hit in enumerate(dense_results):
    print(f"{i+1}. {hit.entity.get('title')} (Score: {hit.distance:.4f})")
    print(f"    路径: {hit.entity.get('path')}")
    print(f"    描述: {hit.entity.get('description')[:100]}...")

print("\n--- [单独] 稀疏向量搜索结果 ---")
sparse_results = collection.search(
    [sparse_vec],
    anns_field="sparse_vector",
    param=search_params,
    limit=top_k,
    expr=search_filter,
    output_fields=["title", "path", "description", "category", "location", "environment"]
)[0]

for i, hit in enumerate(sparse_results):
    print(f"{i+1}. {hit.entity.get('title')} (Score: {hit.distance:.4f})")
    print(f"    路径: {hit.entity.get('path')}")
    print(f"    描述: {hit.entity.get('description')[:100]}...")

print("\n--- [混合] 稀疏+密集向量搜索结果 ---")
# 创建 RRF 融合器
rerank = RRFRanker(k=60)

# 创建搜索请求
dense_req = AnnSearchRequest([dense_vec], "dense_vector", search_params, limit=top_k)
sparse_req = AnnSearchRequest([sparse_vec], "sparse_vector", search_params, limit=top_k)

# 执行混合搜索
results = collection.hybrid_search(
    [sparse_req, dense_req],
    rerank=rerank,
    limit=top_k,
    output_fields=["title", "path", "description", "category", "location", "environment"]
)[0]

# 打印最终结果
for i, hit in enumerate(results):
    print(f"{i+1}. {hit.entity.get('title')} (Score: {hit.distance:.4f})")
    print(f"    路径: {hit.entity.get('path')}")
    print(f"    描述: {hit.entity.get('description')[:100]}...")
```
